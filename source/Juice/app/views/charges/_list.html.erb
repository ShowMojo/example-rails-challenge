<ul class=<%= "#{name}-list" %>>
  <% charges.each do |charge| %>
    <li id=<%= charge.id %>>
      <%= charge.customer.full_name %> |
      <%= charge.amount %> |
      <%= charge.created_at.strftime("%Y-%m-%d") %>
    </li>
  <% end %>
</ul>

