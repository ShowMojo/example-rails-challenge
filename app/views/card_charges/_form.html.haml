= form_for @card_charge do |f|
  - if @card_charge.errors.any?
    #error_explanation
      %h2= "#{pluralize(@card_charge.errors.count, "error")} prohibited this card_charge from being saved:"
      %ul
        - @card_charge.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :created
    = f.datetime_select :created
  .field
    = f.label :paid
    = f.check_box :paid
  .field
    = f.label :amount
    = f.text_field :amount
  .field
    = f.label :currency
    = f.text_field :currency
  .field
    = f.label :refunded
    = f.check_box :refunded
  .field
    = f.label :customer
    = f.text_field :customer
  .actions
    = f.submit 'Save'
