<p id="notice"><%= notice %></p>

<h1>Failed Charges</h1>
<%= render 'charges', charges: @failed, row_class: '.failed' %>
<br>

<h1>Disputed Charges</h1>
<%= render 'charges', charges: @disputed, row_class: '.disputed' %>
<br>

<h1>Successful Charges</h1>
<%= render 'charges', charges: @successful, row_class: '.successful' %>
<br>

<%= link_to 'New Charge', new_charge_path %>
